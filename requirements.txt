streamlit>=1.34  # st.segmented_control (attachment scope picker) needs 1.34+
Pillow  # already a Streamlit dependency, but app.py imports it directly for page_icon/header art
requests
google-genai
anthropic
openai
python-dotenv
pandas
pypdf
playwright  # #173: the board-execution tests EXECUTE the real board in Chromium rather
            # than asserting against its source. Without this package they SKIP, with an
            # honest message that is still easy to miss -- a reclaimed container ran the
            # suite 14 tests lighter and reported OK. A freeze must be licensed by a suite
            # that ran, not one that quietly stood down. The browser itself is
            # pre-provisioned (PLAYWRIGHT_BROWSERS_PATH); do not run `playwright install`.
scipy  # linear_sum_assignment -- exact max-value bipartite matching for lineup_optimizer.py's
       # roster-to-slot assignment; a hand-rolled greedy heuristic can silently mis-assign a
       # multi-eligible player, and that's exactly the number this app's draft engine leans on
